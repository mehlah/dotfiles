# Unix
alias mkdir='mkdir -p'
alias ln='ln -v'
alias ll='ls -al'
alias e="$EDITOR"
alias v="$VISUAL"

# Bundler
alias b="bundle"

# Rubygems
alias gi="gem install"
alias giv="gem install -v"

# Rails
alias s="rspec"

# Local
alias play="cd ~/Playground"
